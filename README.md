# .github

這個 repo 放的是：

- `copilot-instructions.md`、`instructions/`、`prompts/`、`templates/` —— Copilot 用的規範與模板
- `skills/`（`implement`、`plan`、`test`）—— 對應到上面規範的 Claude Code skill，路由到各自的 instructions／prompts
- `profile/README.md` —— GitHub org 首頁
- `examples/` —— 計畫書／簡報範例

大老 fork 來的第三方 skill 包（安裝、更新、組合成 workflow）改由獨立的 [toolkits](https://github.com/MarkovChenITRI/toolkits) repo 管理，不在這裡處理。
