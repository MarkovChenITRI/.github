---
description: "Use when the user asks about releasing or deploying this project. Content not yet established — use `plan` to build out the real procedure once this project's release/deploy tooling is decided."
---

# 發布與部署（Release、Deploy）

這個階段的實際做法（用什麼工具、流程怎麼走、驗收條件是什麼）還沒有建立，因為目前沒有任何依據可以參考，硬寫會是虛構的流程。

使用者需要建立這個階段的內容時，由 `plan` 技能協助盤點這個專案實際採用的發布與部署方式後建立，並回頭幫這份檔案補上對應的 `applyTo`。
