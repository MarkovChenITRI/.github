---
description: "Loaded automatically when working in src/**. Mirrors the implement 技能通用規範 — layer placement, dependency direction, and Implementation SOP pointer."
applyTo: "src/**"
---

# implement 技能通用規範

implement 技能負責 `src/` 底下四個層的核心程式庫，每次異動前先讀取上層資源（規格書）確認邊界；異動後若模組的 What、How、Where 有變，回到規格書更新對應段落。

四層的放置規則與依賴方向，以及 Implementation SOP，詳見 `code-build.instructions.md`。
