---
description: "<觸發語境：動詞 + 受詞 + 產出路徑，從使用者角度描述觸發情境（什麼情況下觸發、產出到哪個路徑）>"
---

# <職責名稱>

<!-- 定位段落：1–2 句，依序說明三件事：
     (1) 職責邊界——這份 SOP 負責什麼；其他職責由哪個技能或 SOP 接手
     (2) 依循哪份 format instructions 的哪個段落
     (3) 產出物的路徑格式
     注意：H1 不含「SOP」字樣。 -->

## 開始之前

<!-- 進入主動作前必須確認的前提，固定三項，依序完成才繼續 -->

1. **確認目標名稱或範圍**：<說明如何確認；使用者未提供時先詢問，確認後才繼續>
2. **確認目標檔案是否已存在**：
   - 已存在且本次要處理的段落有內容：<告知使用者已存在，詢問要整組重新建立、還是局部更新；使用者選定後才繼續>
   - 不存在，或本次段落還沒內容：繼續下方主動作段落。
3. **確認相依的前置資源**：<列出前置文件或設定，例如 overview.md 是否存在、目錄結構是否已選定；資源不存在時停下說明原因，等使用者補齊後再繼續>

## <主動作段落名稱>

<!-- 段落名稱固定三選一：
     - 問答步驟（透過問答收斂內容，最常見）
     - 執行步驟（親自操作系統並記錄結果）
     - 第一階段：<名稱> + 第二階段：<名稱>（需分階段研究後定案）

     問答步驟規則：每個欄位問完先覆誦確認，再問下一個；有順序依賴的欄位（後者的格式依賴前者的結果）說明依賴關係，依序逐一詢問。
     執行步驟規則：逐步操作，每步記下實際觀察（含與預期不符的步驟），不跳步驟。
     多階段規則：每階段用獨立 H2；第一階段結束讓使用者確認才進入第二階段；最後一階段後才進入「寫入」段落。 -->

1. <第一個問題或步驟>
2. <第二個問題或步驟>
3. <第三個問題或步驟，依 SOP 需要增減>

## 寫入 <目標文件種類>

<!-- 段落名稱規則：固定「寫入 + 目標文件種類」
     常見範例：寫入規格書、寫入系統架構、寫入計劃書（敘述段落）、寫入計劃書（圖表段落）、寫入驗收標準、寫入測試紀錄
     子步驟固定三個；「確認」動作是寫入步驟的一部分 -->

1. 依 `<format-instructions>.md`「<對應段落名稱>」的格式，把問答或執行所得內容整理成可寫入狀態。
2. 開啟 `<專案資料夾>/<路徑>/<檔名>.md`，寫入整理好的內容；檔案不存在時先建立。
3. 在對話中貼出寫入的完整內容，請使用者確認。

## 完成條件

<!-- 固定三類，缺一不可，依序列出；每類至少一條 -->

- **格式面**：`<目標檔案路徑>` 已存在，<必填段落或欄位名稱> 齊全——逐一對照 `<format-instructions>.md` 的範本欄位確認。
- **內容面**：<具體性要求，例如「每個欄位的描述有具體依據，讀者能根據這段做出判斷」>；<一致性要求，例如「三段用的分項子項名稱互相一致」（有跨文件對應關係時補上）>。
- **程序面**：使用者已確認過寫入的內容。
