# 全域協作原則

本檔案是這個 `.github` 的全域規則，每次對話都會自動整份載入，不管使用者問什麼、動哪個檔案都要生效，內容分成下列類別：

## 執行品質規範

### 注解文件的論述原則

撰寫程式碼注解或文件說明時，依下列原則決定怎麼寫：

1. 語氣用正向直述句：直接寫清楚這裡是做什麼、為什麼這樣做——用「做什麼」的敘述，取代「不要怎樣」「避免什麼」這種先立一個反面再帶出正面的講法。
2. 描述維持客觀：只寫程式碼實際的行為、限制、原因，用字維持中性。
3. 保持中立的敘述方式：只呈現事實與依據，讓讀者自己判斷；「當然應該」「顯然要」這種已經下好結論的措辭，換成陳述依據的講法。
4. 用詞用台灣中文讀者的日常說法：專有名詞、術語能換成白話說法就換。遇到一定要保留英文原文的專有名詞（例如程式語言關鍵字、框架或函式庫名稱、API 名稱），寫成「白話名稱（英文專有名詞）」的格式——白話名稱放前面，英文原文放在後面的括弧裡。
5. 先核對文件記載，才採用使用者的說辭：規格、使用範例、測試等文件已經記錄的內容是這次撰寫的依據；使用者對現況或需求的描述，角色是界定這次要涵蓋的範圍，本身要先對照文件記載，才能寫進注解或文件。

### 修改後要重新讀取確認

修改既有檔案的方式，統一走 VS Code 提供給 Copilot 的編輯檔案工具（`insert_edit_into_file`；不同版本可能顯示成其他名稱，辨識依據是「直接對開啟或既有檔案套用局部文字修改、並回傳結構化套用結果」的那個工具）。這個工具經由編輯器本身的文字文件模型（Text Document Model）寫入，跟開啟中的編輯器共用同一份緩衝區，檔案的存取權全程留在編輯器手上；寫回時延續該檔案目前偵測到的字元編碼，內容編碼維持與原檔案一致。

1. 編輯檔案工具（`insert_edit_into_file`）呼叫完成後，先確認這次呼叫本身回傳的套用結果：套用結果顯示成功、內容確實比對到要替換的位置時，才進入下一步；套用結果顯示衝突或落空時，處理原因後重新呼叫。
2. 套用結果確認成功後，改用讀取檔案工具（`read_file`）重新開啟目標檔案，逐項核對這次異動的內容是否真的寫入，驗證跟修改走不同的工具路徑。
3. 刪除操作分別確認「檔案」與「目錄本身」兩層現況：用列出目錄的方式確認資料夾本身的清空狀態，用列出檔案的方式確認檔案本身的清空狀態，兩層都個別列出才算完整。
4. 讀取結果跟預期一致時，才能宣稱這次修改已經完成；跟預期不符時，先如實說明落差，修正後回到第 1 步重新走一次確認。

## 流程聯動規範

### 三項維護技能各自對齊上層資源邊界

專案的維護工作依資源類型分成四層，由高到低分別是規格文件、使用範例、測試程式、核心程式庫；前兩層由 `plan` 維護，測試程式由 `test` 維護，核心程式庫由 `implement` 維護。

1. 異動任一層之前，先讀取排在它上面的資源，確認這次使用者要求的異動落在資源已經記載的邊界內。
2. 落在邊界內時，異動內容以上層資源記載的內容為準；使用者這次的敘述，角色是界定這次要異動的範圍。
3. 這一層現有的邏輯跟上層資源記載的目標行為不一致時，先盤點這個行為在這一層所有連動的相關位置，再依上層資源記載的內容，把這整組邏輯統一重新設計；這次異動需要的新增判斷，一併收進這次重新設計的範圍，讓這個行為在這一層維持只有一套邏輯在處理。
4. 超出邊界時，回報這個落差；異動範圍維持在使用者這次明確要求的這一層，上層資源的異動，留給使用者另外提出要求時才進行。
5. 各層具體的檢查步驟，寫在 `plan`、`test`、`implement` 各自指向的 `instructions/*.instructions.md` 或 `prompts/*.prompt.md` 檔案裡，這裡只定義邊界確認的順序。
