# 全域協作原則

本檔案是這個 `.github` 的全域規則，每次對話自動載入，不需依賴 `instructions/` 的 `applyTo` 比對。

## 這份文件收什麼內容

新增內容前，先確認同時符合下列三個條件：

1. 不管使用者在這個專案裡問什麼、動哪個檔案，這條規則都要生效——不是只在特定路徑、特定情境或特定任務類型下才用得到。
2. 內容量要小：這份文件每次對話都會整份載入，份量越大，每次對話要扛的負擔就越重。
3. 這條規則本身不是「怎麼維護這個 `.github`」的內容——那類內容屬於 `instructions/authoring.instructions.md`，只在動到 `.github/instructions/`、`.github/prompts/`、`.github/skills/` 時才載入。

三個條件有任一個不成立，就把內容放進符合條件的機制：路徑觸發放 `instructions/*.instructions.md`，需要明確啟動放 `prompts/*.prompt.md`，語意觸發放 `skills/<name>/SKILL.md`；判斷方式見 `instructions/authoring.instructions.md`。

## 修改後要重新讀取確認

任何檔案修改（新增、編輯、刪除、搬移）完成後，宣稱「已修改」或「已完成」之前，必須用另一個獨立的讀取動作重新檢查目標檔案或目錄的實際狀態，確認變更確實落地，不能只憑修改工具回傳「成功」就結案。

### 為什麼需要這條規則

修改工具有時會回報成功、但實際沒有寫入真實檔案系統；也可能在跟真實環境隔開的封閉環境中執行，讓驗證動作看到的畫面跟真實檔案系統不同步；也可能工具本身在特定情境下無法使用卻沒有明確報錯。只信任工具回傳值，會讓「已完成」的宣稱建立在假設上，而不是事實上。

### 執行方式

1. 修改後，用讀取工具重新開啟或列出目標檔案／目錄，確認內容或存在狀態符合預期。
2. 驗證動作盡量與修改動作走不同路徑（例如換一種工具或換一台機器查證），避免兩者共用同一個會失真的環境，讓驗證失去意義。
3. 刪除操作要同時檢查「檔案」與「目錄本身」兩層：只列檔案的檢查方式（例如只列出檔案、不列目錄的指令）看不到殘留的空資料夾，不能當作已清空的證據。
4. 若驗證結果與預期不符，先如實回報落差，再修正，不得略過驗證直接宣稱完成。
5. 驗證工具本身也可能誤導：例如 Windows PowerShell 5.1 讀取沒有 BOM 的 UTF-8 檔案時，若沒指定用 UTF-8 讀，會用系統內建的字元編碼誤判成亂碼。看到疑似異常結果，先換一種讀取方式交叉確認是內容真的壞了、還是讀取方式本身的問題，再下結論。

這條原則適用於這個專案內所有的 Copilot 請求，不限於特定檔案類型或特定 instructions 範圍。
