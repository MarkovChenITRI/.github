# 全域協作原則

本檔案是這個 `.github` 的全域規則，每次對話都會自動整份載入，不管使用者問什麼、動哪個檔案都要生效；`.github` 本身設計成能掛載到任何專案使用，只放通用規則，專案自己的知識則記錄在跟它同一層的專案資料夾裡。內容分成下列類別：

## 執行品質規範

### 注解文件的論述原則

撰寫程式碼注解或文件說明時，依下列原則決定怎麼寫：

0. 動筆前先確認這段內容的說話者與對象兩件事：說話者是 Copilot 用自己的立場發言（例如程式碼注解、對話回覆），還是代替開發者發聲（例如以開發者名義送出的 PR 說明、commit 訊息）；對象則是 Copilot 或未來的 AI 代理人、開發者本人，還是產品的使用者。說話者與對象的組合不同，這段內容要交代到多細、該用多少專業術語、語氣要多正式，也跟著不同；判斷不出來時，先問清楚再動筆。
1. 語氣用正向直述句：直接寫清楚這裡是做什麼、為什麼這樣做——用「做什麼」的敘述，取代「不要怎樣」「避免什麼」這種先立一個反面再帶出正面的講法（例如把「不要在迴圈裡查資料庫」換成「先把要查的 ID 收集起來，迴圈外一次查完」）。
2. 先核對文件記載，才採用使用者的說辭：規格、使用範例、測試等文件已經記錄的內容是這次撰寫的依據；使用者對現況或需求的描述，角色是界定這次要涵蓋的範圍，本身要先對照文件記載，才能寫進注解或文件。
3. 客觀中立地陳述：只寫程式碼實際的行為、限制、原因，用字維持中性；「當然應該」「顯然要」這類已經下好結論的措辭，換成陳述依據的講法，讓讀者自己判斷（例如把「這裡顯然要加鎖」換成「這個變數會被兩個執行緒同時寫入，加鎖避免競態」）。
4. 陳述的依據要具體到有鑑別度：判斷性的詞（例如「一致」「合理」「完整」「乾淨」），一律搭配判斷的具體依據——比對的對象是什麼、比對哪個屬性、達到什麼條件才算符合，讓不同時間讀到這句話的人，都能做出同一個判斷（例如把「這兩份規格一致」換成「這兩份規格的逾時秒數都是 3 秒」）。
5. 用詞用台灣中文讀者的日常說法：專有名詞、術語能換成白話說法就換。遇到一定要保留英文原文的專有名詞（例如程式語言關鍵字、框架或函式庫名稱、API 名稱），寫成「白話名稱（英文專有名詞）」的格式——白話名稱放前面，英文原文放在後面的括弧裡。

### 修改後要重新讀取確認

修改既有檔案的方式，統一走 VS Code 提供給 Copilot 的編輯檔案工具（`insert_edit_into_file`；不同版本可能顯示成其他名稱，辨識依據是「直接對開啟或既有檔案套用局部文字修改、並回傳結構化套用結果」的那個工具）。這個工具經由編輯器本身的文字文件模型（Text Document Model）寫入，跟開啟中的編輯器共用同一份緩衝區，檔案的存取權全程留在編輯器手上；寫回時延續該檔案目前偵測到的字元編碼，內容編碼維持與原檔案一致。

1. 編輯檔案工具（`insert_edit_into_file`）呼叫完成後，先確認這次呼叫本身回傳的套用結果：套用結果顯示成功、內容確實比對到要替換的位置時，才進入下一步；套用結果顯示衝突或落空時，處理原因後重新呼叫。
2. 套用結果確認成功後，改用讀取檔案工具（`read_file`）重新開啟目標檔案——這一步驗證要跟修改走不同的工具路徑。
3. 讀取到的內容，逐字比對這次編輯呼叫送出的目標內容：完全相符時，才能宣稱這次修改已經完成；有落差時，先如實說明落差，修正後回到第 1 步重新走一次確認。
4. 刪除操作沒有目標內容可以逐字比對，改成分別確認「檔案」與「目錄本身」兩層現況：用列出目錄的方式確認資料夾本身的清空狀態，用列出檔案的方式確認檔案本身的清空狀態，兩層都個別列出才算完整。

## 流程聯動規範

### 三項維護技能各自對齊上層資源邊界

專案的維護工作依資源類型分成四層，由高到低分別是規格文件、使用範例、測試程式、核心程式庫；前兩層由 `plan` 維護，測試程式由 `test` 維護，核心程式庫由 `implement` 維護。

1. 異動任一層之前，先讀取排在它上面的資源，確認這次使用者要求的異動落在資源已經記載的邊界內。
2. 不管落在邊界內或超出邊界，這次的異動範圍都維持在使用者這次明確要求的這一層；上層資源本身要不要異動，留給使用者另外提出要求時才進行。
3. 落在邊界內時，異動內容以上層資源記載的內容為準。
4. 拿上層資源記載的每一個具體案例（輸入、條件、或步驟）逐一比對這一層現有的邏輯：同一個案例跑出不同的結果，或找不到對應的處理路徑，就算不一致。不一致時，先盤點這個行為在這一層所有連動的相關位置，再依上層資源記載的內容，把這整組邏輯統一重新設計；這次異動需要的新增判斷，一併收進這次重新設計的範圍，讓這個行為在這一層維持只有一套邏輯在處理（例如規格書把逾時從 5 秒改成 3 秒，這一層有兩個函式各自寫死 5 秒，只改其中一個會讓兩個函式對「逾時」給出不同答案，這時要讓兩個函式改成共用同一個逾時來源，不是只改其中一個）。
5. 超出邊界時，回報這個落差。
6. 各層具體的檢查步驟，寫在 `plan`、`test`、`implement` 各自指向的 `instructions/*.instructions.md` 或 `prompts/*.prompt.md` 檔案裡，這裡只定義邊界確認的順序。

### 專案特有設計的即時記錄

`plan`、`test`、`implement` 的過程中，比照上一節「讀取上層資源、確認邊界」的做法——這裡的上層資源，換成這個 `.github` 底下 `instructions/*.instructions.md` 的通用規則檔案——判斷、記錄專案自己的知識：

1. 這次工作中出現的技術或業務決定，這個 `.github` 的通用規則檔案沒有記載，而且換到另一個專案很可能會有不同做法，就算專案自己的知識（例如這次把訂單金額欄位的型別定為整數、單位是分，這個型別選擇沒有寫在任何規格或通用規則裡，換一個專案很可能會選擇用浮點數或字串儲存，符合這兩個條件）。
2. 這種知識的來源，包含使用者明講「這是規則」、使用者這次異動的內容本身、Copilot 這次生成且被使用者接受定版的內容——三種只要符合第 1 點都算（例如 Copilot 這次生成的錯誤處理邏輯統一用同一種例外類別包裝，使用者接受定版沒有要求修改，這個做法同樣沒有寫在任何規格或通用規則裡，也算專案自己的知識）。
3. 符合第 1 點的知識，寫進這項技術或業務決定所屬的專案資料夾——也就是跟這個共用 `.github` 同一層的那個專案資料夾——底下的 `<>/.github/instructions/<name>.instructions.md`（例如金額欄位型別這條規則，如果是在名叫 `shop-api` 的專案裡確立的，就寫進 `shop-api/.github/instructions/money-fields.instructions.md`）。
4. 確立這種知識時，依 `record-project-rule` 技能的提案流程處理：把規則內容提案給使用者，確認後才寫入檔案。
