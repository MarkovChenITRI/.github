# 全域協作原則

本檔案是這個 `.github` 的全域規則，每次對話都會自動整份載入，不管使用者問什麼、動哪個檔案都要生效，內容分成下列類別：

## 執行品質規範

### 注解文件的論述原則

撰寫程式碼注解或文件說明時，依下列原則決定怎麼寫：

1. 語氣用正向直述句：直接寫清楚這裡是做什麼、為什麼這樣做，不用「不要怎樣」「避免什麼」這種先立一個反面再帶出正面的講法。
2. 描述維持客觀：只寫程式碼實際的行為、限制、原因，不夾帶個人評價或情緒字眼。
3. 不預設讀者已經認同某個立場：只呈現事實與依據，讓讀者自己判斷，不使用「當然應該」「顯然要」這種已經下好結論的措辭。
4. 用詞用台灣中文讀者的日常說法：專有名詞、術語能換成白話說法就換。遇到一定要保留英文原文的專有名詞（例如程式語言關鍵字、框架或函式庫名稱、API 名稱），寫成「白話名稱（英文專有名詞）」的格式——白話名稱放前面，英文原文放在後面的括弧裡。

### 修改後要重新讀取確認

修改既有檔案一律呼叫 VS Code 提供給 Copilot 的編輯檔案工具（`insert_edit_into_file`；不同版本可能顯示成其他名稱，辨識依據是「直接對開啟或既有檔案套用局部文字修改、並回傳結構化套用結果」的那個工具），不透過終端機的文字取代指令（例如 `sed`、PowerShell 的 `Set-Content`、shell 重新導向）處理既有檔案。這個工具經由編輯器本身的文字文件模型（Text Document Model）寫入，跟開啟中的編輯器共用同一份緩衝區，不會遇到檔案被其他程式佔用而寫入失敗的情況，也會沿用該檔案目前偵測到的字元編碼寫回，不會因為終端機工具的預設編碼跟原檔案不一致而寫出亂碼。

1. 編輯檔案工具（`insert_edit_into_file`）呼叫完成後，先讀取這次呼叫本身回傳的套用結果：結果顯示套用失敗、有衝突、或找不到要替換的內容時，視為修改失敗，處理失敗原因並重新呼叫，才能繼續下一步。
2. 套用結果顯示成功後，改用讀取檔案工具（`read_file`）重新開啟目標檔案，逐項核對這次異動的內容是否真的寫入，用跟修改當下不同的工具做這次核對。
3. 刪除操作要同時檢查「檔案」與「目錄本身」兩層：只列出檔案、不列出目錄的檢查方式看不到殘留的空資料夾，這種結果不能當作已清空的證據。
4. 讀取結果跟預期不符時，先如實說明落差，再進行修正，修正完成後回到第 1 步重新走一次確認，不能省略驗證就直接說已經完成。

## 流程聯動規範

### 日常請求主動比對並套用對應 SOP

這個 `.github` 底下的規則依觸發方式分成三種檔案：路徑觸發的 `instructions/*.instructions.md`、需要手動呼叫的 `prompts/*.prompt.md`，以及語意觸發的 `skills/<name>/SKILL.md`。

1. 使用者的請求沒有明確指名要呼叫哪個 `/<name>` 指令或哪份檔案時，判斷是否符合下列情況。
2. 比對這次請求的情境，跟 `skills/` 底下每一份 SKILL.md 的 `description` 是否相符。
3. 找到相符的 skill 時，直接依它指向的 instructions 或 prompts 檔案內容執行對應 SOP，不必等使用者額外輸入 `/<name>`。
4. 沒有找到相符的 skill 時，維持一般對話方式處理，不強行套用不相關的 SOP。
